import React from 'react';
import { makeStyles } from '@material-ui/styles';
const useStyles = makeStyles({
  root: {}
});

const ClassManager = () => {
  const classes = useStyles();
  return <div>Class Manager</div>;
};

export default ClassManager;
